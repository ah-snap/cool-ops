services:
  api:
    build:
      context: ./server
      dockerfile: Dockerfile
      target: dev
    env_file:
      - ./server/.env
    environment:
      security16Host: ${SECURITY16_HOST:-localhost}
      security16Port: ${SECURITY16_PORT:-1433}
      PGHOST: host.docker.internal
      PGPORT: ${PGPORT:-5433}
      PORT: 3003
      AWS_CONFIG_FILE: /root/.aws/config
      AWS_SHARED_CREDENTIALS_FILE: /root/.aws/credentials
      PORT_FORWARD_MONGO_SSH_KEY_PATH: /run/keys/prodovrckey.pem
      PORT_FORWARD_SNOWDB_SSH_KEY_PATH: /run/keys/snowdb.pem
      PORT_FORWARD_K8S_CONTEXT: ${PORT_FORWARD_K8S_CONTEXT:-}
      PORT_FORWARD_K8S_NAMESPACE: boot-services
      PORT_FORWARD_K8S_SERVICE: cs-license-process-boot
      PORT_FORWARD_K8S_POD: cs-license-process-boot
      PORT_FORWARD_K8S_LOCAL_PORT: "8061"
      PORT_FORWARD_K8S_POD_PORT: "80"
      BUN_WATCH_USE_POLLING: "true"
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"
      CHOKIDAR_INTERVAL: "500"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: bun run dev
    volumes:
      - ./server:/app
      - api_node_modules:/app/node_modules
      - ${USERPROFILE}/.aws:/root/.aws:rw
      - ./prodovrckey.pem:/run/keys/prodovrckey.pem:ro
      - ./snowdb.pem:/run/keys/snowdb.pem:ro
      - ${USERPROFILE}/.kube:/root/.kube:ro
    ports:
      - "3003:3003"
    restart: unless-stopped

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      target: dev
    volumes:
      - ./client:/app
      - client_node_modules:/app/node_modules
      - client_npm_cache:/root/.npm
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:3003/api}
      VITE_D365_BASE_URL: ${VITE_D365_BASE_URL:-http://localhost:8061}
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    ports:
      - "5173:5173"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  api_node_modules:
  client_node_modules:
  client_npm_cache:
