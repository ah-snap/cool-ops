Routes for existing pages /mapping/{commonNameOrMac}
Start focus on search control
Hitting enter on start textboxes should trigger the button
Double check that I added the snow side into the license control (and not just into the assist white label page)
UI elements on portforwards page button that indicates states of forwards 🔴🔵🟢 maybe, could add a ⏩ button to start all

Forward DB ports to host machine so I don't have to forward them anymore
Move forwards to separate image/service from the normal backend service

Page for datastored generic queries

UI to update connect_tier

Potential extra infos for mapping:
    Invite history
    When displaying multiple automation accounts cross out deleted entries
    Lookup by account id

Toggle for staging mode
Staging mode adds
    * Create account in configurable state

Long future nice adds
Websocket events to be able to load pages with info from jira tickets
Split app into plugins so that different teams could use different plugins
